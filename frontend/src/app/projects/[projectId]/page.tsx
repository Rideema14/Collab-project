import { OverviewView } from '@/components/project/views/OverviewView';

/** /projects/:id → the project Overview. Data + chrome come from the layout. */
export default function ProjectOverviewPage() {
  return <OverviewView />;
}
